using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace skildravr.interfaces {
    interface ICanAdd<T>  {

        bool CanAdd(T item){
            if(T || J == null)
        }
    }
}
